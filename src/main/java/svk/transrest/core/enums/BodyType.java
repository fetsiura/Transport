package svk.transrest.core.enums;

/**
 * Purpose:
 * The BodyType enum defines various body types of vehicles.
 */
public enum BodyType {
    COMPACT,
    CONVERTIBLE,
    HATCHBACK,
    COUPE,
    SUV,
    WAGON,
    SEDAN,
    VAN,
    TRANSPORTER,
    OTHER
}
